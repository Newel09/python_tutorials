"""`all_files` package initializer.

This file makes `all_files` a package so its modules can be
executed with `python -m all_files.module` and sibling imports
resolve cleanly when run from the project root.

Keeping this file minimal and empty is fine for small projects.
"""
